apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: zhilian-os
  labels:
    app: web
spec:
  selector:
    app: web
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
