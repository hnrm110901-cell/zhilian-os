# 三品牌 API 对接状态报告

> 检测时间：2026-03-20 10:59
> 检测脚本：`scripts/check_all_apis.py`

## 品智收银 API

### 核心接口状态（6/7 通过）

| 接口 | 状态 | 尝在一起 | 最黔线 | 尚宫厨 |
|------|------|---------|--------|--------|
| 门店信息 organizations.do | ✅ | 5家 | 12家 | 7家 |
| 菜品类别 reportcategory.do | ✅ | 260个 | 159个 | 198个 |
| 支付方式 payment.do | ✅ | 66种 | 64种 | 132种 |
| 每日经营数据 queryOgnDailyBizData.do | ✅ | 3条 | 6条 | 5条 |
| 门店收入汇总 queryOrderSummary.do | ✅ | ✅ | ✅ | ✅ |
| 订单列表 orderNew.do (POST) | ✅ | ✅ | ✅ | ✅ |
| **菜品列表 queryDishesInfo.do** | ❌ | sign error | sign error | sign error |

### 非核心接口

| 接口 | 状态 | 说明 |
|------|------|------|
| 挂账客户 paymentCustomer.do | ✅ | 尝在一起617条, 尚宫厨259条 |
| 做法配料 queryPractice.do | ✅ | 需传ognid，尝36/黔14/厨61条 |
| 桌台信息 queryTable.do | ⚠️ | 三品牌网关均404 |
| 员工列表 queryUserInfo.do | ❌ | 门店id参数名待确认 |

### 门店独立 Token

全部 14 家门店（3+6+5）的独立 Token **全部正常**。

## 微生活 CRM (api.acewill.net)

| 品牌 | 连通性 | 说明 |
|------|--------|------|
| 尝在一起 | ✅ | 交易预览接口连通 |
| 最黔线 | ✅ | 交易预览接口连通 |
| 尚宫厨 | ✅ | 交易预览接口连通 |

CRM 签名验证通过，接口可正常调用。

## 待品智（陈江超）确认的问题

> 品智技术已确认：订单V1用门店token，订单V2用商户token，门店id用omsid

1. **queryDishesInfo.do 签名规则**：POST multipart/form-data 下的签名计算方式与 GET 不同，门店Token/商户Token + 大小写MD5 全部组合均 sign error。需品智提供该接口的签名计算示例代码。
2. **queryUserInfo.do 门店ID参数名**：报"门店id不能为空"，已传 `ognid`、`storeId` 均无效（门店Token和商户Token都试过）。需确认正确的参数名。
3. **404 接口**：`querydishes.do`、`employe.do`、`queryTable.do`、`queryStoreSummaryList.do`、`queryCookingDetail.do` 在三品牌网关均返回 404。是否已更名或移至其他网关路径？
4. **order.do (V1)** 用门店Token调用返回"服务器错误"，需确认参数格式。

## 本次修复内容

1. 品智适配器 `get_dishes()` 增加多路径降级（querydishes.do → queryDishesInfo.do）
2. 品智适配器 `get_employees()` 增加 ognid 参数 + queryUserInfo.do 降级路径
3. 品智适配器 `get_tables()` 增加 ognid 参数
4. 品智适配器 `run_all_checks()` 更新为已验证通过的接口列表
5. 检查脚本修复订单查询必须传 ognid
6. 检查脚本修复微生活 CRM 签名方式（POST multipart + 正确的 sig 算法）
